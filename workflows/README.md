# Package workflows

Put workflows shipped by this package in `<name>/workflow.json` and `<name>/script.js`. The registry treats these as the lowest-precedence scope; user and project definitions can override them.

See the package [README](../README.md#workflow-format) and the `pi-subagents-workflow-authoring` skill before adding one.
